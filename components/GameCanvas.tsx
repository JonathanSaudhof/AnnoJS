import { useEffect } from "react";
import { Stage, Sprite } from "@inlet/react-pixi";
interface Props {}

const GameCanvas = (props: Props) => {
  return <Stage><Sprite image=""></Stage>;
};

export default GameCanvas;
